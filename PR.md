# POSSE課題 PR本文

## 提出URL

- リポジトリ: https://github.com/asahi1117/-my-task-app
- 作業ブランチ: `feat/task-app`（`main` は Vite React テンプレート、実装はこのブランチ）

## stateの一覧

- `tasks`（初期値: localStorage の `"tasks"` があればそれをパースした配列、なければ `[]`）: タスクの配列。1件は `{ id, text, done }`。追加・完了切り替え・削除の本体。
- `input`（初期値: `""`）: 入力欄の文字。打つたびに変わり、追加後は空に戻す。
- `filter`（初期値: `"all"`）: 一覧の絞り込み。`"all"` / `"active"` / `"done"`。

件数（未完了 ○ 件 / 全 ○ 件）は `tasks.filter` と `tasks.length` で毎回計算するので、別のstateにはしていない。

## タスクの追加・完了切り替え・削除で、それぞれどのstateを、どう更新したか

- 追加: `input` を `trim` し、空なら何もしない。通ったときだけ `setTasks([...tasks, { id: Date.now(), text, done: false }])` で新しい配列を渡し、`setInput("")` で入力欄を空にする。
- 完了切り替え: `setTasks(tasks.map(...))` で、対象の `id` だけ `{ ...task, done: !task.done }` に差し替える。
- 削除: `setTasks(tasks.filter((task) => task.id !== id))` で、対象を除いた新しい配列を渡す。

どれも `push` や `splice` で元の配列を触っていない。`filter` state は絞り込みの表示用で、タスク本体の追加・完了・削除では変えていない。

## DOM直接操作を使わずにstateで画面を変えている理由

素のJavaScriptだと、増えた・消えた・打ち消し線、と場所ごとに `querySelector` して自分で書き換えないといけない。タスクが増えるほど「どこを直すか」を追うのが大変で、変数と画面がずれやすい。Reactでは `setTasks` や `setInput` で値を新しくするだけで、画面のどこをどう描くかはReactに任せられる。stateと画面が同じものを見ているので、ボード（画面）とメモ（変数）が分かれない。

## 詰まった場所とどう解決したか

1. 空フォルダへ先に Git を初期化しようとして、Windows 側でシェル起動に失敗した。`pnpm create vite . --template react` でプロジェクトを作ってから `git init` し、`main` にテンプレートを置いて `feat/task-app` を切った。
2. `pnpm run dev` で `Port 5173 is in use`（その先の 5174・5175 も使用中）と出た。すでに別の Vite が動いていたので、空いていた `http://localhost:5176/` で確認した。
3. 自動化の Enter キーだけではフォーム送信が走らなかった。実装は `<form onSubmit={addTask}>` にしてあり、`requestSubmit()` と「追加」ボタンの両方で同じ追加処理が動くことを確認した。キーボードの Enter もこの `onSubmit` を通る。

## 確認結果

### 表示確認

- スマホ幅（375px）: 入力欄と追加ボタンが1行に収まる。フィルター3つも横スクロールなし。タスクカードが見切れていない。
- PC幅（1280px）: 中央の細いカラムに収まる。左右に余白があり、横いっぱいに伸びない。

### 操作確認

- 空文字では追加されない
- ボタンクリックで追加できる
- フォーム送信（Enter と同じ `onSubmit`）で追加できる
- クリックで完了が切り替わり、打ち消し線と薄い色になる
- 削除できる
- すべて / 未完了 / 完了済みのフィルターが動く
- リロード後もタスクが残る

## 判断の記録

今週の実装で判断が必要だった場面:

- 選択肢A: 完了済みを打ち消し線と薄い色で表す
- 選択肢B: 完了済みを背景色だけで表す
- 採用した理由: 打ち消し線は「終わった」が文字そのものから分かる。背景色だけだと、色の差に気づきにくいことがある。丸いチェックも足して、押しやすい場所を確保した。
- 確認方法: 「牛乳を買う」を完了にしたあと、文字に打ち消し線が入り色が薄くなること、未完了の「レポートを書く」は通常の色のままであることをブラウザで確認した。

## AI利用

- 使ったAIツール: Cursor（チャットのコーディング支援）
- 何を依頼したか: Week15 POSSE課題の React タスク管理アプリの実装、Tailwind 導入、ブラウザ確認、GitHub への push と PR 作成
- 自分で修正した箇所: （提出前に自分でタスク名・見た目を確認して直した点を書く）
